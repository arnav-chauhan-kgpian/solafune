# @package _global_
# Exp 3 — Probabilistic head with Gaussian NLL. Model predicts (mean, log_var);
# NLL naturally weights uncertain pixels less, improving heavy-rain calibration.
model:
  probabilistic: true
loss:
  nll_weight: 0.3
